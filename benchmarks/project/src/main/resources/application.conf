app {
  brokerUrl = "localhost:26500"

  starter {
    processId = "benchmark"
    rate = 300
    threads = 2
    bpmnXmlPath = "bpmn/one_task.bpmn"
    payloadPath = "bpmn/big_payload.json"
  }

  worker {
    jobType = "benchmark-task"
    workerName = "benchmark-worker"
    threads = 10
    capacity = 30
    pollingDelay = 1s
    completionDelay = 300ms
  }

  ttStarter {
    processId = "tt"
    rate = 250
    threads = 2
    numTasks = 10
    lastJobType = "responseChecker"

     worker {
        jobType = "benchmark-task"
        workerName = "benchmark-worker"
        threads = 10
        capacity = 30
        pollingDelay = 1s
        completionDelay = 100ms
      }
  }
}